# Copy to secrets.env and fill in (never commit secrets.env)
RESEND_API_KEY=
EMAIL_FROM=Sales <onboarding@resend.dev>
VITE_MAPBOX_TOKEN=
# Optional alias used by the API for geocoding (defaults to VITE_MAPBOX_TOKEN in deploy)
MAPBOX_ACCESS_TOKEN=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
